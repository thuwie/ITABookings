git push https://thuwie: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@github.com/thuwie/ITABookings.git main 
run browser: browser-sync start --proxy "localhost:8080" --files "src/templates/**/*.twig"

export data:
//Chỉ tạo khi kiểm tra nó không có
CREATE USER 'backup'@'%' IDENTIFIED WITH mysql_native_password BY 'backup123';
GRANT ALL PRIVILEGES ON booking.* TO 'backup'@'%';
FLUSH PRIVILEGES;

chạy lệnh này để export
GRANT SELECT, SHOW VIEW, LOCK TABLES, EVENT, TRIGGER, PROCESS ON *.* TO 'backup'@'%';
FLUSH PRIVILEGES;
EXIT;
docker exec mysql_db sh -c "mysqldump -u backup -pbackup123 booking > /tmp/backup_21102025.sql"
docker cp mysql_db:/tmp/backup_21102025.sql .\backup_21102025.sql

//kiểm tra user backup tồn tại:
docker exec -it mysql_db mysql -u root -p
SELECT user, host, plugin FROM mysql.user WHERE user='backup';
